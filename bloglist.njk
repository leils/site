---
layout: siteheader.njk
---
    <h1>everything</h1>
    <hr>
  <ul>
{%- for post in collections.content | reverse -%}
  <li><a href="{{ post.url }}">{{ post.data.title }} -- {{ post.url }}</a>  || 
  {% if post.data.thumbnail %}
    {%set path = post.filePathStem.substring(0, post.filePathStem.lastIndexOf("/") + 1)%}
    {%set thumb = post.data.thumbnail.substring(post.data.thumbnail.lastIndexOf("/") + 1)%}
    <img class="thumbnail" src={{path+thumb}} alt="alt">
  {% endif %}
  </li>
{%- endfor -%}
</ul>
<hr>
